/**
 * Route config for Home
 */

const ROUTES = {
    'app.register': {
        url: '/register',
        views: {
            'content': {
                template: '<register></register>'
            }
        }
    }
};

export default ROUTES;
